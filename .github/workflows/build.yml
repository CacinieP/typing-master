name: Build & Release

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
    branches: [main]

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: windows-latest
            platform: win
          - os: macos-latest
            platform: mac

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install
        env:
          ELECTRON_MIRROR: https://npmmirror.com/mirrors/electron/

      - name: Prepare macOS signing certificate
        if: matrix.platform == 'mac' && env.CSC_LINK != ''
        env:
          CSC_LINK: ${{ secrets.MACOS_CERTIFICATE }}
        run: |
          echo $CSC_LINK | base64 -d > certificate.p12
          security create-keychain -p keychainpassword build.keychain
          security default-keychain -s build.keychain
          security unlock-keychain -p keychainpassword build.keychain
          security import certificate.p12 -k build.keychain -P "${{ secrets.MACOS_CERTIFICATE_PWD }}" -T /usr/bin/codesign
          security set-key-partition-list -S apple-tool:,apple:,codesign: -s -k keychainpassword build.keychain
          rm -f certificate.p12

      - name: Build Electron app (Windows)
        if: matrix.platform == 'win'
        run: npm run build:win

      - name: Configure macOS signing env
        if: matrix.platform == 'mac'
        env:
          CSC_LINK: ${{ secrets.MACOS_CERTIFICATE }}
          CSC_KEY_PASSWORD: ${{ secrets.MACOS_CERTIFICATE_PWD }}
        # 仅当证书 secret 非空时才导出 CSC_* 变量。
        # secret 缺失时变量保持未定义 → electron-builder 自动跳过签名(行为已验证)，
        # 而非注入空字符串导致 "not a file" 错误。
        run: |
          if [ -n "$CSC_LINK" ]; then
            echo "CSC_LINK=$CSC_LINK" >> "$GITHUB_ENV"
            echo "CSC_KEY_PASSWORD=$CSC_KEY_PASSWORD" >> "$GITHUB_ENV"
            echo "::notice::macOS 签名已启用(证书已配置)"
          else
            echo "::notice::macOS 签名已跳过(未配置 MACOS_CERTIFICATE secret)"
          fi

      - name: Build Electron app (macOS)
        if: matrix.platform == 'mac' && github.event_name != 'pull_request'
        run: npm run build:mac
        env:
          # 公证（全部存在时才公证；任一缺失则只签名不公证 —— notarize.js 内部判断）
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_APP_SPECIFIC_PASSWORD: ${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: typing-master-${{ matrix.platform }}
          path: dist/**/*

  release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            artifacts/typing-master-win/*.exe
            artifacts/typing-master-mac/*.dmg
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
